services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: mydiary_user
      POSTGRES_PASSWORD: mydiary_password
      POSTGRES_DB: mydiary_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
